<nav class="navbar navbar-expand-lg navbar-light sticky-top bg-light">
    <div class="container">
      <a class="navbar-brand" href="/">
        <figure class="mb-0">
          <%= image_tag("mcr_logo.png", width: 200) %>
        </figure>
        <%= link_to(login_path, class: "icon_sign_in btn btn-outline-primary mr-2") do %>
              <i class="fa fa-user"></i>
        <% end %>
      </a> 
      <%= form_with url: "/search",  method: :post, class: "d-flex rounded-0 searchbar" do |form| %>
            <%= form.text_field :query, placeholder: "Species name", class: "flex-grow-1" %>
            <%= button_tag type: "submit", class: "btn-search btn btn-primary" do %>
                <i class="fa fa-search"></i>
            <% end %>
      <% end %>
      <div class="collapse navbar-collapse flex-column align-items-end" id="navbarTogglerDemo02">
        <form class="pt-0">
          <% if logged_in_as_admin? %>
            <%= link_to("Sign Out", logout_path, class: "btn btn-outline-secondary mr-2")%>
            
          <% else %>
            <%= link_to(login_path, class: "btn btn-outline-primary mr-2") do %>
              <i class="fa fa-user"></i>
              Admin Login
            <% end %>
          <% end %>
        </form>
      </div>
    </div>
</nav>
